---
name: "house-builder"
description: "使用Minecraft原生fill和setblock命令一键建造房屋"
triggers:
  - "建房"
  - "造房子"
  - "build house"
  - "建造房屋"
  - "房子预设"
auto_trigger: true
source: "house-builder"
author: "FancyHelper Team"
version: "1.0.0"
categories:
  - "preset"
  - "building"
  - "house"
---

# 房屋建造预设

## 插件检查

本预设使用Minecraft原生fill和setblock命令，无需任何插件。

## 使用方法

1. （所有以下操作均由你完成而不是玩家）
2. （重要）通过 `/data get entity <玩家名> Pos` 获取玩家的坐标，执行后你会通过命令返回拿到玩家的坐标。
3. 执行以下顺序，自动建造一个完整的房屋
4. （重要）注意，这些命令一定要分开执行，上一条执行完了活动结果执行再下一条，一次输出最多只能包含一次工具调用
5. （重要）除非用户说明，请勿使用相对坐标（~）
6. 必须调用TODO，持续跟踪进度

## 房屋结构建议

房屋尺寸：7x7x5（长x宽x高）
- 地基：7x7x1
- 墙壁：7x7x4（留出门窗位置）
- 屋顶：7x7x1（斜顶设计）
- 门：位于正面中央
- 窗户：两侧各一个
- 内部：包含床、工作台、箱子、熔炉

## 命令序列（按顺序执行）

【第一步：建造地板】
【第二步：建造四面墙壁】
【第三步：建造屋顶】
【第四步：安装门】
【第五步：安装窗户】
【第六步：内部装饰】
【第七步：外部装饰】

## 注意事项

- 你必须先通过 `/data get entity <玩家名> Pos` 获取玩家的坐标
- /fill命令执行后如果返回"没有方块被填充"，则代表此处已经有正确的方块了
- 合理利用/fill 的 replace 模式是可以事半功倍的
- 如果需要删除某一片区域的方块，可以使用 /fill 命令用 air 替换已建造的区域
- 除非用户明确说明，使用绝对坐标而不是相对坐标
- (重要)注意门分为上下两部分，应分开放置
- (重要)注意床分为两部分，放在一起时应注意朝向
- (重要)注意坐标与空间位置的关系，不要搞错了
- 必须包含房屋的所有部分

## 指令语法

### /fill指令
```
/fill <from> <to> <block> replace [<filter>]
/fill <from> <to> <block> [destroy|hollow|keep|outline|replace]
```

| 模式 | 效果 | 适用场景 |
|------|------|----------|
| destroy | 破坏原有方块（掉落物品）后填充新方块 | 清理区域并获取资源 |
| hollow | 仅填充区域外层，内部填充为空气 | 快速建造空心立方体 |
| keep | 仅替换空气方块，保留原有非空气方块 | 保护现有结构 |
| outline | 仅填充区域外层，内部保持不变 | 为区域添加边框 |
| replace | 默认模式，替换所有方块（无参数时）；加时仅替换指定方块 | 批量替换特定方块 |

### /setblock指令
```
/setblock <x> <y> <z> <block> [replace|destroy|keep]
```

| 模式 | 作用 | 特点 |
|------|------|------|
| replace | 直接替换目标方块 | 不掉落物品，默认模式 |
| destroy | 先破坏原方块再放置 | 会掉落方块物品，相当于手动挖掉再放 |
| keep | 仅在空气位置放置 | 非空气方块不会被改变 |

## 一句话建造房屋示例

玩家可以说："帮我造一个房子"
AI将自动执行上述所有命令序列，建造一个完整的10x8x5（不唯一，根据玩家意愿）房屋，包含：
- 地板（一般情况下地板y坐标为玩家y坐标-1，厚度为1）
- 墙壁（带门窗）
- 斜顶屋顶
- 门和窗户
- 内部家具（床、工作台、箱子、熔炉等），(重要)床、门等分为两部分的方块，请分开放置
- 照明和装饰

## 自定义建造示例

玩家可以说："帮我造一个更大的房子"
AI将调整坐标范围，建造更大的房屋结构。
